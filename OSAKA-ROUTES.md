# 大阪モード v0.2 — 地図・経路・旋回

2026-09-19。Three.js、既存OSMチャンク、Worker形状生成、ストリーミングは維持し、現在位置管理を拡張した。全面的な作り直しはしていない。

## 操作

左上はOSMの通常地図。赤い矢印が現在位置とカメラの向き、青線が今回の経路、黄色い丸が目的地。目的地名も常時表示する。北を上に固定して地名の読みやすさを優先する。地図または拡大ボタンをタップすると全経路に切り替わり、再度タップで現在地追従に戻る。拡大中も3Dの停止ボタンを前面に残す。

開始地点と目的地は6地点から選択できる。進行方向の選択は北端／南端の目的地を選ぶショートカット。高さ・速度・停止・再開・出発地点へ戻る・全画面・計測保存も維持した。「別のルートを選ぶ」で候補を変える。出発地点へ戻る操作は同じルートを再体験するためのもの。到着後の再スタートは別候補を選ぶ。

## 実地図の選択と利用条件

Leaflet 1.9.4をローカル同梱し、OSM標準ラスターマップを表示する。自作の模式図・推測した道路地図ではない。MapLibreの追加WebGLコンテキストやベクタースタイル配信を導入するより小さな追加で済む。LeafletのBSD-2-Clauseライセンスを [LEAFLET-LICENSE.txt](LEAFLET-LICENSE.txt) に保存した。

[Leaflet公式API](https://leafletjs.com/reference.html) と [OSMタイル利用ポリシー](https://operations.osmfoundation.org/policies/tiles/) を確認した。OSMタイルは表示範囲だけ取得し、keepBuffer=0、HTTPキャッシュを通常どおり使用する。一括取得・オフライン保存・全経路のタイル先読み・キャッシュ回避は行わない。通常のブラウザUser-AgentとRefererを送る。実地図のクレジットを地図内に表示し、全画面でも残す。

**v0.1との通信上の違い：** 3D・経路データは引き続き静的な同一サイト配信だが、ミニマップ画像だけ `tile.openstreetmap.org` へのアクセスがある。仮想走行位置に対応するタイル座標がサービスへ伝わる。端末のGPSは取得しない。OSMの配信は無保証で、アクセス増加時は別の適切な地図配信先への移行が必要。失敗時は地図上にエラーを表示し、3D走行を止めない。OSMの誤りの報告先は [地図の問題を報告](https://www.openstreetmap.org/fixthemap)、アプリ連絡先は [GitHub Issues](https://github.com/smilebest2/shiten-approach-lab-dev/issues)。

## 一つの現在位置

`OsakaRouteEngine.snapshot(manifest)` がx/z、緯度経度、yaw、経路距離、移動距離、高さ、速度、道路名を返す。各描画フレームでこの同じスナップショットを3Dカメラと地図に渡す。地図側に別の速度積分や進行タイマーを持たせない。

地図の中心・経路・タイル再配置は最大5Hz。その間のフレームは、同じ現在座標の投影差をCSS変換に使う。地図を地理的にずらしたり未来位置を予測したりしない。矢印の座標と向きは3Dと同じ更新に追従する。拡大中は全経路を固定表示し、現在位置だけ動く。

## 実道路の候補生成

新たな外部経路APIは使わず、既存のOSMスナップショットから `prepare-osaka-routes.mjs` で事前生成する。OSMの共有ノードで接続された4,541ノードの道路グラフを使用。主要道路・一般道・住宅街道路を含み、階段・歩道・私道／通行不可指定・地下・通常の高架道は除外する。渡河する橋は使用するが、既存の平面地形方針に合わせて路面を平坦化する。

最短経路を基準にし、既に使ったエッジに重みを加えて別候補を探す。狭い道路には追加コストを与える。ノードを再訪しない経路だけを残し、最短候補の1.5倍を超える迂回を除外する。全15地点ペアに計57候補、`osaka/routes.json` 約1.10MB。逆方向も同じ実道路で走れる仮想体験であり、現実の一方通行・転回規則を再現する経路案内ではない。

難波〜梅田は4.21 / 4.41 / 5.29 / 5.06kmの4候補。通常の選択では曲がりのある3候補を使い、直前と同じ候補を避ける。候補ごとの大きな方向変化は3 / 5 / 11回。ルート計算は事前に終わっており、スマートフォンでグラフ探索を繰り返さない。

新規生成：リポジトリルートで `node prepare-osaka-routes.mjs`。元ファイルは `osaka/source.json.gz` と `osaka/manifest.json`。派生データもOSMのODbLで公開する。[データライセンス](osaka/LICENSE.md)。

## 曲がりと場所の識別

実道路の交差点内だけを小さな二次ベジェ曲線でつなぐ。前後の辺長の20%、推定道幅の45%、12mのうち最小値を接線方向の切り取り量とする。これによりカメラ座標の角を丸め、街区を横断する大きな曲線にはしない。カメラは速度に応じて前方10〜32mを見通し、向きの更新を最大2rad/sに制限する。60fpsでは最大約1.91度／フレーム。

元道路中心線から交差点内で少しずれる。道幅が推定値のため、すべての建物・縁石との衝突回避を保証するものではない。300km/hの急カーブは横方向の流れが強く、現実の車両力学とも異なる。揺れは追加していない。

映像内に最寄りの地区名、現在の道路名、近くにある名前付きの川・橋・公園・駅・建物を軽量なテキストで表示する。地区は既存チェックポイント、施設等334件は元OSMのnameタグとgeometryに基づく。いずれも **「案内表示」** と明記し、実在の看板や店舗ロゴを捏造しない。全候補が必ず全チェックポイントの真上を通るわけではなく、「周辺」は最寄り地区を示す。

## 検証と限界

`npm test`で既存の接近・大阪テストも維持。追加テストは57候補の全エッジのOSM隣接ノードとの一致、ノード再訪なし、迂回上限、全候補の往復到着、現在地チャンク保持、回転上限、同一候補の連続選択回避を確認する。

ブラウザ結果は [OSAKA-ROUTES-QA.json](OSAKA-ROUTES-QA.json)。曲がりの多い約5.05km、高さ1m、300km/hで測る。地図のマーカー座標と3Dの位置を走行中に毎フレーム比較する。通常ブラウザで実際のOSM地図表示を別途目視確認した。自動テストはOSMの公共サービスを走査しないよう、タイル応答を100ms遅延のテスト画像に置換する。**テスト画像は製品に含めない。** Leaflet・SVGルート・位置更新・3D・同時通信の処理は動かしたまま測定するが、実配信の混雑とPNG内容の負荷を完全には再現しない。

390×844・DPR3（3D描画1.25）・CPU4倍低速化・4Mbps/100msの測定はPC上のエミュレーション。iPhone/Android実機、SafariのGPU・発熱・省電力は未検証。場所の認識しやすさ、旋回の気持ちよさ、映像酔いは人による最終評価が必要。フォトリアル化や対象地域の拡張は行っていない。

|約5.05km、11か所の方向変化を含む全区間|PC 1440×1100|モバイル画面＋制限条件|
|---|---:|---:|
|平均FPS|59.96|55.31|
|95%フレーム間隔|16.8ms|33.3ms|
|50ms超フレーム|0|17|
|直近の街の準備完了|1.01秒|9.84秒|
|JSヒープ観測最大|26.20MiB|29.01MiB|
|保持チャンク最大|23|23|
|現在地チャンク未読フレーム|0|0|
|カメラと共有現在地の最大差|0m|0m|
|地図マーカーと共有緯度経度の最大差|0m|0m|

初回の20Hz地図再配置では制限条件の平均49.49fpsだった。5Hz再配置＋座標差のCSS変換へ変更した後、55.31fpsになった。同一マシンの逐次測定であり厳密な統計実験ではない。3Dを含めて常時60fpsになるとは断定しない。開始待ちは経路ファイルと近傍の街データ取得を含む。

追加確認：全画面と地図拡大中の停止、任意の目的地への逆向き選択、地図タイルHTTP503時の3D継続、タップによる地図復旧。追従地図の現在位置矢印は描画上の中心から0.36px以内（単回のスモーク測定）。地理座標の完全一致とは別に、Leafletのピクセル丸めによる差を確認した。
