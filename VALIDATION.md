# 検証結果・残っている品質課題

2026-09-17 / 試作v0.1。技術的な接近表示は成立した。人間の緊張感とスマートフォン実機での滑らかさは、まだ最終合格としない。

## 測定条件

- Windows、Edge 153のheadlessブラウザ、WebGL2。
- GPU: Intel UHD Graphics 620 / ANGLE Direct3D11。
- PC: 1440×1080、DPR1。スマホ相当: 390×844、端末DPR3をエミュレーションし描画DPRを1.5へ制限。
- 実測は毎フレームのrequestAnimationFrame間隔。直近最大1,200フレームを保持。平均FPS = 1,000 / 平均間隔。p95はフレーム時間の95百分位。
- JSヒープは`performance.memory.usedJSHeapSize`。GPU・DOM・ブラウザ全体のメモリを表さず、GCのタイミングでも変わる。
- 同じPCのCPU/GPUを使ったモバイルエミュレーション。スマートフォンの実測と取り違えない。
- 初期プロトタイプでFPSを高めに算出していた処理を修正し、以下は修正後の値。

## 4視点の測定

|視点|PC 平均FPS|PC p95 ms|PC JS heap MB|縦長表示 平均FPS|縦長 p95 ms|縦長 JS heap MB|
|---|---:|---:|---:|---:|---:|---:|
|球体|60.0|16.8|8.5|59.1|16.8|7.9|
|犬|60.0|16.8|10.9|59.2|16.8|10.7|
|虫|60.0|16.9|11.1|59.2|16.8|10.0|
|惑星|60.0|16.8|10.4|59.3|16.8|12.0|

開始前のシーン準備完了まで: 通常ローカル配信で初回774ms、別ブラウザコンテキストで350ms。これは初回ページロードの値で、各シナリオの切替時間ではない。

追加の模擬負荷: 10Mbps・遅延80ms・キャッシュ無効・CPUを4倍低速化。準備完了3,509ms、犬シナリオ59.7FPS、p95 16.8ms、50ms超フレーム0、JSヒープ7.3MB。同じPC GPUなので低価格スマホ相当の保証にはならない。

初回に読み込むHTML/CSS/JSは非圧縮合計約0.76MB。大半が同梱Three.js。画像、モデル、外部フォントのダウンロードはない。HTML以外の計測リソース一覧を[QA-extra.json](QA-extra.json)へ保存。公開回線の測定は別途必要。

## 接近の品質判定

|判定項目|確認内容|現時点の判定|
|---|---|---|
|1. 遠方では小さい|基本球体は半径1、中心距離24。開始視角は約4.8°。開始画面で小さい対象を確認。|技術確認済み|
|2. 自然な拡大|対象の寸法は固定し、カメラの位置を前進。視角の単調増加を自動検証。|技術確認済み|
|3. 終盤の急拡大|拡大率を時系列保存。中盤と終端を撮影し、球と葉と惑星が視界を覆うことを確認。|幾何学・描画は確認済み。主観評価は未実施|
|4. 衝突寸前の緊張|対象直前で制動、直撃は接触直前で静止。足元／葉の終端を確認。|人間の「緊張感」は未判定|
|5. 異なるスケール|物理換算0.025〜3,390,000で同一の正規化軌道・角度。各景色の画面も確認。|投影の共通性は確認済み。体感の同等性は未判定|
|6. スマホの滑らかさ|390×844、タッチUI相当で約59FPS、横スクロールなし。320px幅でも横はみ出しなし。|レイアウト確認済み。実機FPSは未検証|

「3Dが表示できたから最終完成」とは扱わない。本試作は接近の知覚評価に進める段階。

## 操作と共通エンジン

- 全4視点で開始 → 一時停止 → 位置不変 → 再開 → 手前で停止をブラウザ操作で確認。
- 回避コースをUIで選び、接触せず通過を確認。リセット、視点切替、比率比較、背景ON/OFF、JSON保存を確認。
- 全画面へ移った後の一時停止を確認。OSの動き低減設定で勝手に移動が始まらないことを確認。
- ページ内JavaScriptエラー、対象リソースの404は0。最初にあったfaviconの404も専用アイコン追加で解消。
- 4視点×4コースをNodeで検証。球体の停止余裕0.08に対し実測0.08016単位、回避と横切りは最短0.08単位。
- 犬の回避は箱の対角線を余裕として使うので最短0.17単位、葉は0.09単位。0.08に必ず一致する仕様ではない。
- 20単位/秒＋加速6の大きい時間刻みでも衝突をすり抜けず、接触手前約0.002単位で静止。
- 高い視点では物体を通過する。対象が視線の下にあるのに誤って衝突させない。
- シナリオを12回切替後、共通球のGPU登録geometryは6個へ戻った。JSヒープは約19〜21MBでGC前の一時オブジェクトを含む。長時間メモリ漏れがないことまで保証する試験ではない。

## 未検証・既知の制約

1. Android Chrome、iPhone Safari実機、Firefox、macOS Safari。特に熱でクロックが下がる長時間使用。
2. 「怖い／迫る／自分が動いた」の人間による比較。作者側の画像確認で代用しない。
3. 手の持ち方と画面の視距離による効果。仮想FOVと実際の視野占有率は異なる。
4. 犬・葉の正確な輪郭に沿った衝突。現在は保守的な箱、画面の角度値は外接球。
5. 180秒を超える極端な低速設定は終了する。100msを超えるフレーム遅延は移動量を制限し、飛び越しを避ける。この場合、実時間より体験時間が遅れる。
6. 公開回線・特定スマートフォンでの起動時間。上の低速条件はDevToolsによる模擬。
7. 緊急停止はクリック/タップ時点で更新を止める設計だが、実機タッチから表示までの入力遅延は未測定。

## 実機で行う次の試験

中価格AndroidとiPhoneの各1台以上で、公開URLの初回起動・全4視点・横向き/縦向き・3分の繰返し・停止/再開を実施。画面の計測JSONへ機種・OS・ブラウザ・回線・体感を追記。暫定目標は平均50FPS以上、p95 25ms以下、0.1秒を超える引っ掛かりを減らすこと。達しない場合は描画DPRを1.0へ下げ、三角形や草の数を減らして原因別に再検証する。

知覚試験の条件と拡張案は[RESEARCH.md](RESEARCH.md)。まず背景の有無とFOVを比較し、効果が弱いときにシナリオ数を増やしてごまかさない。

測定の生データ: [QA-results.json](QA-results.json)、[QA-extra.json](QA-extra.json)。
